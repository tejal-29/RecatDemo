// react.js is the javascript librbry it is use to build UI (frontend) , made by Meta in 2013 
// component based 
// variables are the containers
// Library - GSAP (animations), lenis (smooth scroling), reactJS (UI ) 
// library used for specific feaures
// framework - nextJS, angular
// frmework is inbuild we cant change specificly 

//  1] import and export : - two types -- Default and named
//  2] Real dom vs virtual dom :-  1) in Real dom there are drabacks when we are trying to change anythig in the webpage
//  we need to change entire web for the specific changes , need to rerender entire webpage 
//         Real Dom 

        //     HTML
        //     /   \
        //    /     \
        //   head    Body
        //     /         \
        //    /           \
        //   title        header , section1, section2 , footer   
        //                             |
        //                             |
        //                           h1,  Image, button

//   2) Virtual Dom :- Virtual dom make a copy of real dom and see where are the changes amd then it will change that specific change without re rendering entire web page      
// witout update the entire page


// 3] JSX : it is the combination of html and javascript

// 4] Component : it is the reusable code which we can use in our webpage , it is like a function which return some UI
// 5] Props : it is the way to pass data from one component to another component  always from parent to child component , it is read only we cant change the props in the child component
// 6] State : it is the way to manage the data in a component 
// 7] Hooks : it is the way to use state and other react features in functional components      
//